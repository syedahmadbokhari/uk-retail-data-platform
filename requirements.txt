# ── Core runtime ──────────────────────────────────────────────────────────────
streamlit
pandas
plotly
scikit-learn
numpy
scipy
statsmodels
matplotlib
openpyxl
SQLAlchemy>=2.0
psycopg2-binary

optuna>=3.6
mlflow>=3.0

# ── Orchestration (install separately or via Docker image) ────────────────────
# apache-airflow[postgres]>=2.8   # heavy install — use docker compose up instead

# ── dbt transformation layer ──────────────────────────────────────────────────
dbt-core>=1.7,<2.0
dbt-postgres>=1.7,<2.0

# ── Cloud data warehouse (optional — only needed when GOOGLE_CLOUD_PROJECT is set) ──
google-cloud-bigquery>=3.0

# ── Cloud data warehouse (optional — only needed when SNOWFLAKE_ACCOUNT is set) ──
snowflake-connector-python>=3.0
